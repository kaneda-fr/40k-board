import { ClasssementparpositionPipe } from './classsementparposition.pipe';

describe('ClasssementparpositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClasssementparpositionPipe();
    expect(pipe).toBeTruthy();
  });
});
